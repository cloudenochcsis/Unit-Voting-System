<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Military Unit Voting System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #1a2639;
            color: #d8dbe2;
            font-family: 'Courier New', monospace;
        }
        .container {
            max-width: 800px;
            margin-top: 50px;
        }
        .card {
            background-color: #2c3e50;
            border: 2px solid #e74c3c;
            margin-bottom: 20px;
        }
        .card-header {
            background-color: #e74c3c;
            color: #f0f0f0;
            font-weight: bold;
        }
        .military-header {
            text-transform: uppercase;
            letter-spacing: 2px;
            font-weight: bold;
            border-bottom: 2px solid #e74c3c;
            padding-bottom: 10px;
        }
        .btn-primary {
            background-color: #3d5a80;
            border-color: #3d5a80;
        }
        .btn-primary:hover {
            background-color: #2c3e50;
            border-color: #2c3e50;
        }
        .error-icon {
            font-size: 4rem;
            color: #e74c3c;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="text-center mb-4">
            <h1 class="military-header">System Error</h1>
        </div>
        
        <div class="card p-4 text-center">
            <div class="error-icon">
                ⚠️
            </div>
            <h2 class="mb-4">An Error Occurred</h2>
            
            <p class="mb-3"><%= message %></p>
            
            <% if (error && Object.keys(error).length > 0) { %>
                <div class="alert alert-danger text-start">
                    <h5>Error Details:</h5>
                    <pre><%= JSON.stringify(error, null, 2) %></pre>
                </div>
            <% } %>
            
            <div class="d-grid mt-4">
                <a href="/" class="btn btn-primary">Return to Dashboard</a>
            </div>
        </div>
        
        <div class="mt-4 text-center">
            <p><small>FOUO - For Official Use Only</small></p>
            <p><small>Authorized Personnel Only</small></p>
            <p><small>Please report this error to system administrators</small></p>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
